# conventions.md — MOVED

Project conventions are the **rules layer** of the three-file contract and live in the consuming project, not inside a skill.

**Starter template:** `templates/design.md` at the repo root.
**Per-project path:** set `designDocPath` in `.claude/figma-config.json`.

See the README for the full contract explanation. This file is kept only to break old links; edit the template or your project's `design.md`, never this file.
